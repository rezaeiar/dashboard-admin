export type notificationLength = {
    notificationLength: { value: number }
}

export type ChangePasswordModalType = {
    changePasswordModal: {
        value: {
            visibility: boolean
        }
    }
}