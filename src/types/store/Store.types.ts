export type NotificationLengthType = {
    notificationLength: {
        value: number
    }
}

export type ChangePasswordModalType = {
    changePasswordModal: {
        value: {
            visibility: boolean
        }
    }
}