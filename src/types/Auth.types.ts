export type singUpType = {
    first_name: string,
    last_name: string,
    email: string,
    username: string,
    password: string
}

export type singInType = {
    username: string,
    password: string
}