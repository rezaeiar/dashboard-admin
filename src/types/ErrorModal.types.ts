export type errorModalType = {
    errorModal: {
        value: {
            visibility: boolean,
            payload: {
                title: string,
                description: string
            }
        }
    }
}