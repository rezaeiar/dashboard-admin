export type errorModalType = {
    errorModal: {
        value: {
            vissablity: boolean,
            payload: {
                title: string,
                description: string
            }
        }
    }
}